import * as vscode from 'vscode';
import type { AwarenessState, ActivityEvent, ActivityEventType } from '@campfires/shared';
export interface ConnectionState {
    connected: boolean;
    reconnecting: boolean;
}
export type ConnectionCallback = (state: ConnectionState) => void;
export type AwarenessCallback = (states: Map<number, AwarenessState>) => void;
export type ActivityCallback = (events: ActivityEvent[]) => void;
export declare class AwarenessProvider implements vscode.Disposable {
    private doc;
    private provider;
    private activityFeed;
    private disposables;
    private userId;
    private displayName;
    private teamId;
    private token;
    private serverUrl;
    private color;
    private isDraftMode;
    private isIdle;
    private currentFile;
    private currentFunction;
    private currentBranch;
    private connectionCallbacks;
    private awarenessCallbacks;
    private activityCallbacks;
    private lastFileSave;
    private lastFileOpen;
    constructor(options: {
        userId: string;
        displayName: string;
        teamId: string;
        token: string;
        serverUrl: string;
        color: string;
    });
    connect(): void;
    private setupActivityListener;
    disconnect(): void;
    updateAwareness(): void;
    private getStatus;
    setDraftMode(enabled: boolean): void;
    setIdle(idle: boolean): void;
    setCurrentFile(file: string | null): void;
    setCurrentFunction(func: string | null): void;
    setCurrentBranch(branch: string | null): void;
    getDraftMode(): boolean;
    pushActivityEvent(type: ActivityEventType, options?: {
        file?: string;
        branch?: string;
        message?: string;
        metadata?: Record<string, unknown>;
    }): void;
    private shouldEmitEvent;
    getAwarenessStates(): Map<number, AwarenessState>;
    getActivityFeed(): ActivityEvent[];
    getConnectedCount(): number;
    isConnected(): boolean;
    onConnectionChange(callback: ConnectionCallback): void;
    offConnectionChange(callback: ConnectionCallback): void;
    onAwarenessChange(callback: AwarenessCallback): void;
    offAwarenessChange(callback: AwarenessCallback): void;
    onActivityChange(callback: ActivityCallback): void;
    offActivityChange(callback: ActivityCallback): void;
    private notifyConnectionCallbacks;
    private notifyAwarenessCallbacks;
    private notifyActivityCallbacks;
    dispose(): void;
}
